{
  "name": "@scaling-memory/electron-example",
  "version": "1.0.0",
  "description": "Electron example using LLM layer for content analysis",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@scaling-memory/shared": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
